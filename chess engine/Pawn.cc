#include "lib/Pawn.h"

bool Pawn::isMoved(){
    return this->moved;
}

/* Pawn::setMoved(){
    this->moved = true;
}
Pawn::move(int column, int row){
    setColumn(column);
    setRow(row);

}
Pawn::getPossibleNextMove(Board board){
if (isMoved)
    
} */